# 📦 Автоматизация GitHub релизов для BadPhone

Это руководство описывает процесс создания и опубликования релизов приложения BadPhone на GitHub.

## 🚀 Быстрый старт

### 1. Получить токен доступа GitHub

1. Перейти на https://github.com/settings/tokens
2. Нажать "Generate new token (classic)"
3. Установить следующие разрешения:
   - `repo` (полный доступ к репозиториям)
   - `read:packages` (если нужно)
4. Скопировать сгенерированный токен

### 2. Установить переменную окружения

**Windows PowerShell:**
```powershell
$env:GITHUB_TOKEN = "your_token_here"
```

**Windows CMD:**
```cmd
set GITHUB_TOKEN=your_token_here
```

**Постоянно (Windows):**
```powershell
[Environment]::SetEnvironmentVariable("GITHUB_TOKEN", "your_token_here", "User")
```

### 3. Создать релиз

**Способ 1: Полный процесс (рекомендуется)**
```powershell
./release.ps1 1.0.7
```

Этот скрипт:
- ✅ Обновит версию в `package.json`
- ✅ Создаст коммит с сообщением о версии
- ✅ Загрузит изменения на GitHub
- ✅ Создаст git тег `v1.0.7`
- ✅ Соберёт приложение (electron-builder)
- ✅ Создаст релиз на GitHub
- ✅ Загрузит артефакты (.exe файлы)

**Способ 2: Только создание релиза (если версия уже обновлена)**
```powershell
node scripts/create-github-release.js --version 1.0.7 --draft false
```

## 📋 Команды скрипта `create-github-release.js`

### Основные параметры

| Параметр | Короткая форма | Описание | Пример |
|----------|---|----------|---------|
| `--version` | `-v` | Номер версии (обязательно) | `--version 1.0.7` |
| `--draft` | `-d` | Создать как черновик (default: true) | `--draft false` |
| `--prerelease` | `-p` | Отметить как предварительный (default: false) | `--prerelease true` |
| `--artifact` | `-a` | Путь к артефакту для загрузки | `--artifact dist/BadPhone.exe` |
| `--notes` | `-n` | Пользовательские заметки релиза | `--notes "See changelog.md"` |

### Примеры использования

**Создать релиз-черновик без артефактов:**
```powershell
node scripts/create-github-release.js -v 1.0.7
```

**Создать финальный релиз с одним артефактом:**
```powershell
node scripts/create-github-release.js -v 1.0.7 -d false -a dist/BadPhone-Setup-1.0.7.exe
```

**Создать предварительный релиз с несколькими артефактами:**
```powershell
node scripts/create-github-release.js -v 1.0.7-beta -p true `
  -a dist/BadPhone-Setup-1.0.7-beta.exe `
  -a dist/BadPhone-1.0.7-beta.zip
```

**Создать релиз с пользовательскими заметками:**
```powershell
node scripts/create-github-release.js -v 1.0.7 -d false `
  -n "Major features: New dashboard, Performance improvements"
```

## 📁 Структура процесса релиза

```
./release.ps1 1.0.7
    ↓
    ├─ Обновить версию в package.json
    ├─ Создать git коммит
    ├─ Загрузить на GitHub
    ├─ Создать git тег
    ├─ Собрать приложение (electron-builder)
    │  └─ Артефакты: dist/
    └─ Создать GitHub релиз
       └─ Загрузить артефакты
```

## 🔍 Структура создаваемого релиза

**URL релиза:**
```
https://github.com/7teenth/BadPhone/releases/tag/v1.0.7
```

**Структура GitHub релиза:**
```
📦 v1.0.7
├─ 📝 Описание (автогенерированное или пользовательское)
├─ 📎 BadPhone-Setup-1.0.7.exe (~50 MB)
├─ 📎 BadPhone-1.0.7.zip (портативная версия)
└─ 📅 Дата: 21 января 2026
```

## 📝 Примечания к релизу

Скрипт автоматически генерирует примечания релиза с:
- Датой выпуска
- Типичными улучшениями (можно кастомизировать)
- Требованиями к системе
- Ссылками на все релизы

## ❌ Решение проблем

### "GITHUB_TOKEN не установлен"
```powershell
$env:GITHUB_TOKEN = "your_token_here"
# или используйте постоянную переменную окружения
```

### "HTTP 401: Bad credentials"
- Проверьте, что токен скопирован полностью
- Убедитесь, что токен не истёк
- Создайте новый токен

### "HTTP 404: Not Found"
- Проверьте написание `owner` и `repo` в скрипте
- Убедитесь, что репозиторий существует

### "HTTP 422: Validation Failed"
- Версия должна быть в формате `X.Y.Z`
- Тег `vX.Y.Z` не должен уже существовать
- Проверьте описание релиза на спецсимволы

### Артефакты не загружаются
- Убедитесь, что файлы существуют
- Проверьте права доступа к файлам
- Размер файла < 2 GB

## 🔐 Безопасность токена

⚠️ **ВАЖНО:**
- Никогда не коммитьте токен в репозиторий
- Используйте переменные окружения или `.env` файл
- Регулярно ротируйте токены
- Удалите старые токены на GitHub

## 📚 Дополнительно

- [GitHub API Documentation](https://docs.github.com/en/rest/releases/releases)
- [Electron Builder](https://www.electron.build/)
- [Octokit.js](https://octokit.github.io/rest.js/)

## 🎯 Workflow CI/CD

Для полной автоматизации создайте GitHub Actions workflow:

```yaml
name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm install
      - run: npx electron-builder --win
      - run: node scripts/create-github-release.js --version ${{ github.ref_name }} --draft false --artifact dist/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

---

**Автор:** GitHub Copilot
**Проект:** BadPhone POS
**Дата обновления:** 21 января 2026
